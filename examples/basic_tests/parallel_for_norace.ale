// No race: mutex-protected accumulation
procedure main(argc, argv)
    shared mutable counter := 0
    shared mutable mtx as mutex := mutex()

    parallel for i := 0 to 100 with 4 thread do
        lock(mtx)
        counter := counter + 1
        unlock(mtx)
    end parallel for

    print `counter = {counter}`
end procedure
