// Intentional race: all threads write to the same counter without a mutex
procedure main(argc, argv)
    shared mutable counter := 0

    parallel for i := 0 to 100 with 4 thread do
        counter := counter + 1
    end parallel for

    print `counter = {counter}`
end procedure
