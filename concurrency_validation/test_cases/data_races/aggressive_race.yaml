# Test case: Thread leak demonstration
# Threads are created but join_threads may not wait for all properly
# Also has unprotected shared counter access (data race)

expected_issues:
  - data_race
  - thread_leak
timeout: 15
