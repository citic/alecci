# SCTBench benchmark: concurrent-software-benchmarks/account_bad.c
# Three threads share one mutex protecting balance.
# BUG: wrong assertion formula balance == (x-y)-z instead of x+y-z.
# With x=1, y=2, z=4 the assertion fires when check_result sees both flags set.
# Logic bug, not a concurrency bug — TSan finds no races.
# Expected: no concurrency issues (program may abort due to bad formula).
expected_issues:
  - bug
timeout: 10
