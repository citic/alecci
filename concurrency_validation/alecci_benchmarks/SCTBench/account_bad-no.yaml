# SCTBench benchmark: concurrent-software-benchmarks/account_bad.c
# Three threads share one mutex protecting balance.
# The original C code contains a wrong assertion formula (logic bug, not a concurrency bug).
# Alecci has no assert — the logic error cannot be represented.
# Expected: no concurrency issues (TSan cannot detect logic bugs, only races).

expected_issues:
  - none
timeout: 10
