# SCTBench benchmark: concurrent-software-benchmarks/reorder_3_bad.c
# 2 set threads write shared variables; 1 check thread reads them without synchronization.
# Expected: data race detected by TSan.
expected_issues:
  - bug
timeout: 10
