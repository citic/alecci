# SCTBench benchmark: concurrent-software-benchmarks/din_phil2_sat.c
# 2 dining philosophers with unprotected shared counter 'phil'.
# Ground truth (buggy.txt): listed as buggy — data race on 'phil'.
# Note: TSan may not consistently observe the race with only 2 threads;
# if undetected this is a MISS. TSan's lock-order-inversion is extra/unexpected.
# Expected: data race on 'phil' (source ground truth).

expected_issues:
  - data_race
timeout: 10
