# SCTBench benchmark: concurrent-software-benchmarks/reorder_20_bad.c
# 10 set threads write shared variables; 10 check threads read them without synchronization.
# Expected: data race detected by TSan.
expected_issues:
  - bug
timeout: 10
