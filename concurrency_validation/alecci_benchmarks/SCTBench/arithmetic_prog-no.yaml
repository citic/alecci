# SCTBench benchmark: concurrent-software-benchmarks/arithmetic_prog_ok.c
# Producer-consumer with sum accumulation over N=4 iterations via two condvars.
# All shared accesses (num, total) are mutex-protected — no data race.
# Expected: no concurrency issues.

expected_issues:
  - none
timeout: 10
