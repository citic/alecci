# SCTBench benchmark: inspect_examples/sync01.c
# Minimal 1-slot producer-consumer: single produce-consume cycle via two condvars.
# All shared accesses are mutex-protected — no data race.
# Expected: no concurrency issues.
expected_issues:
  - none
timeout: 10
