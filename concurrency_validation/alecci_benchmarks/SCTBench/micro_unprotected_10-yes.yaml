# SCTBench benchmark: concurrent-software-benchmarks/micro_10_ok.c
# Ten threads perform 100 unprotected increments on a shared counter.
# Expected: data race detected by TSan.

expected_issues:
  - data_race
timeout: 10
