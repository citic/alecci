# SCTBench benchmark: concurrent-software-benchmarks/reorder_5_bad.c
# 4 set threads write shared variables; 1 check thread reads them without synchronization.
# Expected: data race detected by TSan.

expected_issues:
  - data_race
timeout: 10
