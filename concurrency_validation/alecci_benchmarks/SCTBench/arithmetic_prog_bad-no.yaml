# SCTBench benchmark: concurrent-software-benchmarks/arithmetic_prog_bad.c
# Producer-consumer accumulation with N=3. The original C has a wrong assert
# (always fires): total != N*(N+1)/2. Added — program always aborts with
# assertion message. Logic bug, not a concurrency bug — TSan finds nothing.
# Expected: no concurrency issues (program aborts due to bad assertion formula).
expected_issues:
  - bug
timeout: 10
