# SCTBench benchmark: concurrent-software-benchmarks/din_phil6_sat.c
# 6 dining philosophers with unprotected shared counter 'phil'.
# Ground truth (buggy.txt): listed as buggy — data race on 'phil'.
# Note: TSan also reports lock-order-inversion on fork mutexes; this is extra
# detection beyond the ground-truth bug (TSan false positive on fork ordering).
# Expected: data race on 'phil' (source ground truth).
expected_issues:
  - bug
timeout: 10
