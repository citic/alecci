# SCTBench benchmark: concurrent-software-benchmarks/sync01_bad.c
# 1-slot producer-consumer, but consumer never decrements num.
# The producer loops forever waiting on 'empty' — condvar deadlock.
# Ground truth: buggy (buggy.txt). TSan may miss — condvar deadlocks
# do not produce a TSan deadlock report; program times out.

expected_issues:
  - deadlock
timeout: 5
